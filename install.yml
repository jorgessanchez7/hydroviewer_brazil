# This file may be committed to your app code.
version: 1.2
# This should match the app - package name in your setup3.py
name: hydroviewer_brazil

requirements:
  # Putting in a skip true param will skip the entire section. Ignoring the option will assume it be set to False
  skip: false
  conda:
    channels:
      - conda-forge
    packages:
      - netCDF4
      - gdal
      - geoglows
      - hydrostats
      - pandas
      - requests
      - plotly
      - numpy
      - scipy
      - bs4
      - lxml
      - xmltodict
  pip:

post:
